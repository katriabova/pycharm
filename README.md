# Виджет банка

## Тестирование

Для запуска тестов используется библиотека `pytest`.
Команда для запуска:
`python -m pytest`


read_transactions_csv и read_transactions_excel, теперь проект поддерживает форматы .csv и .xlsx

